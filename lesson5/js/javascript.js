let results;
let acct = prompt('Please choose from our options Q = Quit, W = Withdraw, D = Deposit, B = Balance');

function doubler(numberToDouble) {
  results = numberToDouble * 2;
}
doubler(acct);
alert(results);
