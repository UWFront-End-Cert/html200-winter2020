/*The calculator will need 4 variables; use all the operators and console log the
result of using the two variables. Mix and match the variables in the operators.*/

//The calculator will need 4 variables
let w = 0
let x = 1
let y = 2
let z = 3

/*Use all the operators and console log the result using the two variables. Mix
and match the variables in the operators.*/
console.log(w * x);
console.log(x / y);
console.log(y + z);
console.log(z - y);
console.log(y % x);
