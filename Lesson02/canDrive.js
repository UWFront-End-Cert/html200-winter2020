const firstName = 'Jake';
const lastName = 'Liu';
let age = 13;
let drivingAge = 16;

if (age >= drivingAge) {
  console.log(firstName + ' ' + lastName + ' can drive, and ' + (age - drivingAge) + ' years he have driven.');
} else {
  for (let i = 5; i >= 0; i--) {
    console.log(i);
  }
}
/*
const firstName = "James";
const lastName = "Norton";
const age = 43;
const drivingAge = 16;
const drivingTime = age - drivingAge;
//check to see if i am legal to drive
if (age >= drivingAge) {
    // do codes
    console.log(firstName + ' ' + lastName + ' ' + 'can drive and has driven for ' + drivingTime + ' years')
} else {
  for (let i = 5; i > 0; i--) {
    console.log(i);
}

}
// if i am print James Norton can drive and has driven for x years

// if i am not legal to drive console log down from s

*/
