let firstName = "Bob";
let lastName = "Rogers";
let age = 22;
let limit = 16;
let x = (age - limit);
if (age >= limit) {
  console.log(firstName + ' ' + lastName ' you have been driving for' + x + ' years.');
}
