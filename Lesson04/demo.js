function numberSquarer() {
    let input = prompt('Enter a number to be squared');
    let numberToSqaure = 0;
    let result = 0;
    
    result = Number(input) * Number(input);
    
    alert(result);
}

function numberMultiplier() {
    let input1 = prompt('Enter the first number to multiply');
    let input2 = prompt('Enter the second number to multiply');
    let numberMultiplier = 0;
    let answer = 0;

    answer = Number(input1) * Number(input2);

    alert(answer);
}

