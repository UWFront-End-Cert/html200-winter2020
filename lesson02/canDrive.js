const firstName = 'Boubacar'
const lastName = 'Diallo';
let age = 15;
let drivingAge = 16;
if (age >= drivingAge) {
  console.log(firstName + ' ' + lastName + ' is eligible to drive')
} else {
  console.log(firstName + ' ' + lastName + ' is not eligible to drive')
} for (let i=5; i >=0; i--) {
    console.log(i)
}
